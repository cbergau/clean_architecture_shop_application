sudo: required

services:
  - docker

before_script: docker run --rm -v $(pwd):/app composer/composer install

script:
  - wget https://scrutinizer-ci.com/ocular.phar

after_success:
  - docker run --rm -v $(pwd):/var/www/html cbergau/clean_architecture_shop_symfony3client php ./vendor/bin/phpunit --coverage-clover=coverage_clover.xml --configuration=tests/ tests/ && php vendor/bin/coveralls -x coverage_clover.xml -v
